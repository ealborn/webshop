@model List<ArtShop.Project.Core.Models.CartViewModel>
@{
    ViewData["Title"] = "Index";
}

<h2>My shopping cart</h2>

<div class="cartWrap">
    @foreach (var item in Model)
    {
        Html.RenderPartial("Cart", item);
    }
</div>
